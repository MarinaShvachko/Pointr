package pages;

import org.openqa.selenium.WebDriver;

//TODO when a page with article is opened to get all words create an object of article page
public class ArticlePage extends BasePage {
    public ArticlePage(WebDriver driver) {
        super(driver);
    }
}
